org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# ---------------------------------------------------------------------------
# The web app URL baked into the APK at build time (BuildConfig.WEBAPP_URL).
#
# Set it here, or pass -PWEBAPP_URL="https://..." on the command line, or edit
# android/app/build.gradle. If unset/empty the default below is used.
# ---------------------------------------------------------------------------
# WEBAPP_URL=https://your-server.example
