<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for the WebView wrapper.

  The web app is normally served over HTTPS; the base-config below only
  trusts system CAs and forbids cleartext. The domain-config entries grant
  cleartext exceptions for development / preview hosts that are still HTTP
  (local emulator, LAN testing, and preview hosts whose certs may not be
  public). If the production server you bake into the APK is HTTP-only,
  add its host to the domain list below.
-->
<network-security-config>
    <!-- Allow cleartext for development / preview URLs -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">*.freebuff.app</domain>
        <domain includeSubdomains="true">*.freebuff.com</domain>
    </domain-config>

    <!-- Trust system CAs for HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
